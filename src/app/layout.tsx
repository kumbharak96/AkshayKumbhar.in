import type { Metadata } from "next";
import { Plus_Jakarta_Sans, Geist_Mono } from "next/font/google";
import "./globals.css";

const plusJakartaSans = Plus_Jakarta_Sans({
  variable: "--font-sans",
  subsets: ["latin"],
  weight: ["400", "500", "600", "700", "800"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Akshay Kumbhar | Video Editor & AI Ads Creator | Motion Graphics Portfolio",
  description: "Explore the premium video editing, AI ads creation, motion graphics, and content editing portfolio of Akshay Kumbhar. Delivering cinematic pacing and high-retention marketing assets.",
  keywords: ["Video Editor", "AI Ads Creator", "Motion Graphics", "Cinematic Storytelling", "Reels Editor", "Akshay Kumbhar"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${plusJakartaSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
